# 成绩管理与复议系统 — 开发文档

> **日期：** 2026-07-31  
> **项目：** SmartAdmin 3.0.0 / Java 17 / SpringBoot 3  
> **开发者：** 王浩哲  
> **基础 URL：** `http://localhost:1024`

---

## 一、项目概述

在 SmartAdmin 框架基础上，新增了三个业务模块，构成一套完整的考试成绩管理与复议系统。

### 模块总览

```
smart-admin-api-java17-springboot3/
└── sa-admin/.../business/
    ├── score/     ← 成绩管理（16 文件）
    ├── appeal/    ← 复议管理（10 文件）
    └── ai/        ← AI 外部接口（6 文件）
```

### 数据流

```
学生答题 → t_answer_detail (gradeType=2)
              │
              ▼
Python AI → POST /api/external/ai/pending-answers  拉取待批改数据
              │
              ▼
Python AI → POST /api/external/ai/submit-grade     提交评分(含三维度)
          → POST /api/external/ai/submit-feedback   提交学习建议
              │
              ▼ 自动重算总分 → t_score
              │
              ▼
学生 → POST /score/score/detail      查看成绩(含AI评分+复议状态)
     → POST /score/score/history     查看历史成绩
     → POST /appeal/appeal/submit    提交复议(≤500字+3张图)
     → POST /appeal/appeal/query     复议进度列表
     → GET  /appeal/appeal/{id}      复议详情(含教师处理结果)
```

---

## 二、成绩管理模块 `score/`

### 2.1 目录结构

```
score/
├── controller/ScoreController.java        2 个接口
├── service/
│   ├── ScoreService.java                  重算总分
│   └── ScoreQueryService.java             查询+JSON解析+复议关联
├── manager/ScoreAiManager.java            Mock评分(真实数据优先)
├── dao/                                  3 个 DAO(MyBatis-Plus)
├── entity/                               3 个实体(t_score/t_student_answer/t_answer_detail)
├── vo/                                   4 个响应对象
├── form/ScoreQueryForm.java              查询表单
└── constant/PassStatusEnum.java          及格枚举
```

### 2.2 接口

| 接口 | 方法 | 用途 |
|------|------|------|
| `/score/score/detail` | POST | 查看本次成绩详情 |
| `/score/score/history` | POST | 查看历史成绩列表 |

### 2.3 功能说明

**成绩详情 `POST /score/score/detail`**

请求：
```json
{"examId": 1}
```
可选参数 `studentId`，不传则取当前登录用户。

响应包含：
- 总分、及格状态、排名、交卷/批改时间
- 每题得分：原始分 × 难度系数 = 加权分
- 评分理由（30-80字）
- **三维度评分明细**：相关性、知识点覆盖度、逻辑表达性（各0-5分+评语）
- **AI 整体学习建议**：总体评价 + 亮点 + 薄弱点 + 改进建议
- **复议状态**：每题是否已申请复议、复议状态（待审核/已通过/已驳回）

**Mock/真实数据自动切换机制**：
- 优先读取数据库中的 JSON 数据（Python 通过外部接口写入）
- JSON 解析成功 → 使用 Python AI 真实数据
- JSON 解析失败或为空 → 回退到 `ScoreAiManager` Mock 数据
- 无需任何配置切换，查询时自动判断

**历史成绩 `POST /score/score/history?studentId=1`**

返回该学生所有考试成绩，按交卷时间倒序，包含每场考试是否有复议记录。

### 2.4 涉及的数据库表

| 表名 | 字段 |
|------|------|
| `t_score` | score_id, exam_id, student_id, total_score, pass_status, rank_position, submit_time, grade_time, remark |
| `t_student_answer` | answer_id, exam_id, student_id, status, verify_fail_count, verify_lock_until, submit_time |
| `t_answer_detail` | id, answer_id, question_id, user_answer, voice_record_url, score, grade_type, grade_remark |

---

## 三、复议管理模块 `appeal/`

### 3.1 目录结构

```
appeal/
├── controller/AppealController.java       4 个接口
├── service/
│   ├── AppealService.java                 提交复议 + 防重复校验
│   └── AppealQueryService.java            分页查询 + 详情
├── dao/AppealDao.java                     t_appeal 表
├── entity/AppealEntity.java               复议实体
├── vo/AppealVO.java + AppealDetailVO.java 列表项 + 详情
├── form/AppealSubmitForm.java + AppealQueryForm.java
└── constant/AppealStatusEnum.java         状态枚举
```

### 3.2 接口

| 接口 | 方法 | 用途 |
|------|------|------|
| `/appeal/appeal/submit` | POST | 提交复议申请 |
| `/appeal/appeal/checkSubmitted?answerDetailId=1` | GET | 检查某题是否已申请 |
| `/appeal/appeal/query` | POST | 分页查询复议记录 |
| `/appeal/appeal/{appealId}` | GET | 查看复议详情 |

### 3.3 功能说明

**提交复议 `POST /appeal/appeal/submit`**

请求：
```json
{
  "scoreId": 1,
  "answerDetailId": 1,
  "examId": 1,
  "appealReason": "我对该题评分有异议，理由如下..."
}
```

业务规则：
- 复议理由必填，最多 **500 字**（`@Size(max=500)`）
- 可上传证据截图，最多 **3 张**（先调 `/file/upload` 拿到 URL）
- 同一道题**不可重复提交**（代码层校验 + `@Size(max=3)` on evidenceUrls）
- 提交后状态变为「待审核」

**复议列表 `POST /appeal/appeal/query`**

请求：
```json
{"pageNum":1, "pageSize":10}
```
可选筛选：`examId`、`status`

排序：待审核排最前，其余按申请时间倒序。

**复议详情 `GET /appeal/appeal/{appealId}`**

返回完整复议信息，包含：
- 原题目内容、学生答案
- 复议理由 + 证据截图
- 教师处理意见、调整后分数
- 处理状态（待审核/已通过/已驳回）+ 处理时间

### 3.4 涉及的数据库表

| 表名 | 关键字段 |
|------|---------|
| `t_appeal` | appeal_id, score_id, answer_detail_id, exam_id, student_id, appeal_reason, evidence_urls, status, old_score, new_score, teacher_remark, teacher_id, handle_time |

---

## 四、AI 外部接口 `ai/`

### 4.1 目录结构

```
ai/external/
├── controller/AiExternalController.java   3 个端点
├── service/AiExternalService.java         数据查询/写入/重算
└── dto/
    ├── PendingAnswerResponse.java         待批改答案
    ├── GradingSubmitRequest.java          评分提交
    ├── FeedbackSubmitRequest.java         学习建议提交
    └── AiExternalResultResponse.java      统一响应
```

### 4.2 接口

| 序号 | 接口 | 用途 |
|------|------|------|
| ① | `POST /api/external/ai/pending-answers` | 拉取待批改答案列表 |
| ② | `POST /api/external/ai/submit-grade` | 提交单题评分（含三维度） |
| ③ | `POST /api/external/ai/submit-feedback` | 提交整体学习建议 |

### 4.3 功能说明

**① 拉取待批改答案**

查询条件：`grade_type=2`（外部服务）且 `score IS NULL`（未批改）且作答已提交。

请求 `{}`，返回数组：
```json
[{
  "answerDetailId": 1001,
  "examId": 10,
  "studentId": 2001,
  "questionId": 5001,
  "userAnswer": "人工智能在医疗领域...",
  "voiceRecordUrl": null,
  "gradeType": 2
}]
```

`answerDetailId` 在提交评分时必须原样回传。

**② 提交单题评分**

```json
{
  "answerDetailId": 1001,
  "score": 8.5,
  "gradeRemark": "答题完整，逻辑清晰",
  "relevanceScore": 4.5,
  "relevanceComment": "高度相关",
  "coverageScore": 4.0,
  "coverageComment": "覆盖全面",
  "logicScore": 4.0,
  "logicComment": "逻辑通顺"
}
```

提交后自动：写入 score + grade_remark（JSON）、重算总分、更新作答状态。

**③ 提交学习建议**

```json
{
  "examId": 10,
  "studentId": 2001,
  "overallComment": "整体表现良好",
  "highlights": "概念准确",
  "weaknesses": "深度不足",
  "improvementSuggestions": "多做练习"
}
```

### 4.4 数据存储格式

评分数据以 JSON 存入 `t_answer_detail.grade_remark`：
```json
{"remark":"...","relevance":{"score":4.5,"comment":"..."},"coverage":{...},"logic":{...}}
```

学习建议以 JSON 存入 `t_score.remark`：
```json
{"type":"ai_feedback","overallComment":"...","highlights":"...","weaknesses":"...","improvementSuggestions":"..."}
```

---

## 五、鉴权说明

**当前状态**：所有接口通过 `@NoNeedLogin` 注解跳过登录校验，方便测试。

**正式上线恢复方式**：
1. 去掉每个 Controller 方法上的 `@NoNeedLogin`
2. 恢复 `@SaCheckPermission` 注解
3. 不需要改 `MvcConfig`（已恢复原始拦截器配置）

---

## 六、Postman 测试

> 所有接口无需 token，`Content-Type: application/json`，基础 URL `http://localhost:1024`

### 6.1 AI 评分流程（Python 模拟）

```
① POST /api/external/ai/pending-answers
   Body: {}
   → 记下 answerDetailId

② POST /api/external/ai/submit-grade
   Body: {"answerDetailId":1,"score":8.5,"gradeRemark":"...","relevanceScore":4.5,"relevanceComment":"...","coverageScore":4.0,"coverageComment":"...","logicScore":4.0,"logicComment":"..."}

③ POST /api/external/ai/submit-feedback
   Body: {"examId":1,"studentId":1,"overallComment":"整体优秀","highlights":"...","weaknesses":"...","improvementSuggestions":"..."}
```

### 6.2 成绩查询

```
④ POST /score/score/detail
   Body: {"examId": 1}
   → 验证 questions[].relevanceScore 是 Python 提交的值（非 Mock）
   → appealed 字段来自 t_appeal 表（非固定 false）

⑤ POST /score/score/history?studentId=1
   → 返回历史成绩数组，含 hasAppeal 字段
```

### 6.3 复议流程

```
⑥ POST /file/upload
   Body: form-data → key:file(选择图片), key:folder(Text:1)
   → 记下返回的 URL

⑦ POST /appeal/appeal/submit
   Body: {"scoreId":1,"answerDetailId":1,"examId":1,"appealReason":"理由..."}

⑧ GET /appeal/appeal/checkSubmitted?answerDetailId=1
   → true/false

⑨ POST /appeal/appeal/query
   Body: {"pageNum":1,"pageSize":10}
   → 列表按待审核优先排序

⑩ GET /appeal/appeal/1
   → 含 status/teacherRemark/newScore(教师处理后)
```

### 6.4 完整测试链

```
Step 1-3 : AI 评分流程(①②③)
Step 4   : 查成绩详情 → 确认 AI 评分 + 复议状态(false)
Step 5   : 查历史成绩
Step 6   : 上传证据截图
Step 7   : 提交复议
Step 8   : 确认已提交
Step 9   : 查复议列表 → 状态=待审核
Step 10  : 查复议详情 → 拉教师处理结果
Step 11  : 再次查成绩详情 → 确认 appealed=true
```

### 6.5 数据库验证

```sql
-- 评分是否写入
SELECT id, score, grade_remark FROM t_answer_detail WHERE id = 1;
-- grade_remark 为 JSON 格式

-- 成绩是否自动生成
SELECT * FROM t_score WHERE exam_id = 1 AND student_id = 1;

-- 学习建议
SELECT remark FROM t_score WHERE exam_id = 1 AND student_id = 1;
-- remark 为 {"type":"ai_feedback",...}

-- 复议记录
SELECT * FROM t_appeal WHERE student_id = 1 ORDER BY create_time DESC;
```

---

## 七、改动文件清单

### 新增（32 个文件）

| 模块 | 文件 | 说明 |
|------|------|------|
| score | `ScoreController.java` | 成绩详情 + 历史成绩 |
| score | `ScoreService.java` | 重算总分 |
| score | `ScoreQueryService.java` | 查询+JSON解析+复议关联 |
| score | `ScoreAiManager.java` | Mock评分 |
| score | `ScoreDao.java` | DAO |
| score | `StudentAnswerDao.java` | DAO |
| score | `AnswerDetailDao.java` | DAO |
| score | `ScoreEntity.java` | 成绩实体 |
| score | `StudentAnswerEntity.java` | 作答记录实体 |
| score | `AnswerDetailEntity.java` | 答案详情实体 |
| score | `ScoreDetailVO.java` | 成绩详情VO |
| score | `ScoreQuestionVO.java` | 单题得分VO |
| score | `ScoreAiFeedbackVO.java` | AI建议VO |
| score | `ScoreHistoryVO.java` | 历史成绩VO |
| score | `ScoreQueryForm.java` | 查询表单 |
| score | `PassStatusEnum.java` | 及格枚举 |
| appeal | `AppealController.java` | 复议接口 |
| appeal | `AppealService.java` | 提交+校验 |
| appeal | `AppealQueryService.java` | 查询+详情 |
| appeal | `AppealDao.java` | DAO |
| appeal | `AppealEntity.java` | 复议实体 |
| appeal | `AppealVO.java` | 列表VO |
| appeal | `AppealDetailVO.java` | 详情VO |
| appeal | `AppealSubmitForm.java` | 提交表单 |
| appeal | `AppealQueryForm.java` | 查询表单 |
| appeal | `AppealStatusEnum.java` | 状态枚举 |
| ai | `AiExternalController.java` | 外部接口 |
| ai | `AiExternalService.java` | 数据查询/写入 |
| ai | `PendingAnswerResponse.java` | 待批改DTO |
| ai | `GradingSubmitRequest.java` | 评分DTO |
| ai | `FeedbackSubmitRequest.java` | 建议DTO |
| ai | `AiExternalResultResponse.java` | 结果DTO |

### 修改（1 个文件）

| 文件 | 改动 |
|------|------|
| `MvcConfig.java` | 无改动（已恢复原始状态） |

---

## 八、待完成事项

| # | 事项 | 说明 |
|---|------|------|
| 1 | 创建考试/题目/课程表 | 目前 examTitle/courseName/questionContent 等字段为 null |
| 2 | AI 出题模块 | 表结构和接口已就绪，Python 侧可扩展 |
| 3 | 人脸/声纹验证 | 表字段已预留（verifyFailCount/voiceRecordUrl） |
| 4 | 正式上线恢复权限 | 去掉 @NoNeedLogin，恢复 @SaCheckPermission |
| 5 | 复议截止时间 | 需在考试表中增加字段 |

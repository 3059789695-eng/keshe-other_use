# 教师端后端模块

> 考试完成情况、成绩统计与复议处理

> 使用说明

| 负责人 | 马翔 |
| --- | --- |
| 用户故事 | T-04 / T-05 / T-08 / T-08-M |
| 后端地址 | http://localhost:1024 |
| 接口文档 | http://localhost:1024/doc.html |
| 日期 | 2026-08-01 |

# 1. 模块说明

本模块只负责教师端后端功能：查看考试完成情况、统计成绩、查看答卷、导出成绩，以及查询和处理成绩复议。Web 与移动端共用同一套复议接口。

说明：项目只有后端和数据库脚本，没有前端工程；测试使用 1024 端口的 Knife4j 接口文档和 Postman。

# 2. 文件位置

| 成绩统计代码 | backend/sa-admin/src/main/java/net/lab1024/sa/admin/module/business/score/ |
| --- | --- |
| 复议代码 | backend/sa-admin/src/main/java/net/lab1024/sa/admin/module/business/appeal/ |
| Mapper | backend/sa-admin/src/main/resources/mapper/business/score/、appeal/ |
| SQL | 数据库SQL脚本/mysql/maxiang_exam_module.sql |
| Postman | backend/docs/马翔负责模块.postman_collection.json |

# 3. 安装与启动

环境：JDK 17、Maven 3.6+、MySQL 8、Redis。数据库名为 smart_admin_v3。

1. 先执行 smart_admin_v3.sql，再执行 maxiang_exam_module.sql。
2. 在 sa-base/src/main/resources/dev/sa-base.yaml 中配置 MySQL 地址、用户名和密码。
3. 启动 MySQL 和 Redis，Redis 执行 ping 应返回 PONG。
4. 在 IDEA 中运行 sa-admin 模块的 AdminApplication。
5. 打开 http://localhost:1024/doc.html，能看到接口文档即启动成功。

数据库：127.0.0.1:3306/smart_admin_v3
Redis：127.0.0.1:6379

# 4. Postman 使用

1. 导入 马翔负责模块.postman_collection.json。
2. 先发送 0-教师登录并自动保存 Token。
3. 该请求会自动获取验证码、登录并保存 token。
4. 再依次发送编号 1 至 9 的教师端业务请求。

```text
Authorization: Bearer <token>
```

注意：token 会自动保存，不需要手工填写。重新导入集合前应删除 Postman 中的旧版本。

# 5. 接口清单

| 序号 | 功能 | 方法 | URL | 权限 |
| --- | --- | --- | --- | --- |
| 1 | 分页查询考试 | POST | /score/exam/query | score:statistics:query |
| 2 | 查看统计数据 | GET | /score/statistics/detail/{examId} | score:statistics:query |
| 3 | 查询学生完成情况 | POST | /score/student/query | score:statistics:query |
| 4 | 查看学生完整答卷 | GET | /score/answer/detail/{examId}/{studentId} | score:statistics:query |
| 5 | 导出全部成绩 | GET | /score/statistics/export/{examId} | score:statistics:export |
| 6 | 导出未完成名单 | GET | /score/unfinished/export/{examId} | score:statistics:export |
| 7 | 分页查询复议 | POST | /appeal/application/query | appeal:application:query |
| 8 | 查看复议详情 | GET | /appeal/application/detail/{appealId} | appeal:application:query |
| 9 | 通过或驳回复议 | POST | /appeal/application/review | appeal:application:review |

# 6. 请求与输出示例

## 6.1 查询考试

POST /score/exam/query

```json
{
  "pageNum": 1,
  "pageSize": 10,
  "semesterId": 1,
  "courseId": 1,
  "examStatus": 2,
  "examName": ""
}
```

## 6.2 处理复议

POST /appeal/application/review

```json
{
  "appealId": 1,
  "reviewResult": 1,
  "adjustedScore": 15,
  "teacherOpinion": "同意调整为15分。"
}
```

reviewResult：1 通过，2 驳回。驳回时可以不传 adjustedScore。

## 6.3 成功返回

```json
{
  "code": 0,
  "msg": "操作成功",
  "ok": true,
  "data": {}
}
```

# 7. Excel 导出

接口 5 和 6 返回 .xlsx 文件。Postman 中出现 PK、[Content_Types].xml 等二进制内容是正常现象。点击 Send 右侧下拉箭头，选择 Send and Download。

1. 全部成绩保存为 考试成绩.xlsx。
2. 未完成名单保存为 未完成学生名单.xlsx。
# 8. 验收与提交

| 检查项 | 结果 |
| --- | --- |
| 自动登录并保存 token | 通过 |
| 6 个成绩统计接口 | 通过 |
| 3 个成绩复议接口 | 通过 |
| 两类 Excel 导出 | 通过 |

# 9. 常见问题

1. No database selected：执行 USE smart_admin_v3。
2. RedisConnectionException：启动 Redis。
3. 接口提示未登录：先运行 Postman 教师登录请求。
4. 导出显示二进制字符：使用 Send and Download 保存为 .xlsx。
# 10. 提交内容

1. 后端代码和 Mapper XML。
2. smart_admin_v3.sql 与 maxiang_exam_module.sql。
3. Postman 集合和本使用说明。
4. 上传前删除个人 token、真实数据库密码和临时日志。
